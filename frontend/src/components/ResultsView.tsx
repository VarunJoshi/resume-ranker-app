import React from 'react';

const ResultsView: React.FC = () => {
  return (
    <div style={{ marginTop: '2rem' }}>
      <h3>📊 Ranked Results (coming soon!)</h3>
      <p>This is where resume scores will show up.</p>
    </div>
  );
};

export default ResultsView;
